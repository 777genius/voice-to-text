# WebSocket E2E Tests

Комплексные e2e тесты для WebSocket networking (Deepgram и AssemblyAI провайдеры).

## Структура тестов

### 1. Подключение и Реконнект

- **`test_e2e_deepgram_websocket_connection`** - Базовое подключение к Deepgram WebSocket
- **`test_e2e_assemblyai_websocket_connection`** - Базовое подключение к AssemblyAI WebSocket
- **`test_e2e_deepgram_reconnect`** - Переподключение после остановки
- **`test_e2e_multiple_sequential_connections`** - Множественные последовательные подключения (проверка утечек памяти)
- **`test_e2e_abort_during_active_connection`** - Abort во время активного соединения

### 2. Обработка Сообщений и Ошибок

- **`test_e2e_deepgram_message_handling`** - Получение и обработка partial/final транскрипций
- **`test_e2e_connection_error_invalid_key`** - Обработка ошибки с неверным API ключом ✅ (unit test)
- **`test_e2e_connection_timeout_handling`** - Обработка таймаута подключения
- **`test_e2e_server_initiated_close`** - Обработка закрытия соединения от сервера

### 3. Сценарии со Слабым Интернетом

- **`test_e2e_slow_network_simulation`** - Медленная отправка данных (задержки 300-500ms)
- **`test_e2e_batch_sending`** - Отправка больших пачек данных (500ms чанки)
- **`test_e2e_high_frequency_sending`** - Быстрая отправка данных (10ms интервалы, стресс-тест)
- **`test_e2e_keepalive_mechanism`** - Keep-Alive механизм при длительной паузе
- **`test_e2e_recovery_after_connection_loss`** - Восстановление после разрыва соединения

### 4. Комплексные Сценарии

- **`test_e2e_long_session_with_pauses`** - Длинная сессия с периодическими паузами (реальный use case)
- **`test_e2e_performance_comparison`** - Сравнение производительности Deepgram vs AssemblyAI

## Запуск тестов

### Все unit тесты (без реального API)
```bash
cargo test --test websocket_e2e_test test_e2e_connection_error
```

### Все e2e тесты с реальным API
```bash
cargo test --test websocket_e2e_test -- --ignored --nocapture
```

### Конкретный тест
```bash
cargo test --test websocket_e2e_test test_e2e_deepgram_websocket_connection -- --ignored --nocapture
```

### Тесты подключения
```bash
cargo test --test websocket_e2e_test connection -- --ignored --nocapture
```

### Тесты обработки сообщений
```bash
cargo test --test websocket_e2e_test message -- --ignored --nocapture
```

### Тесты слабого интернета
```bash
cargo test --test websocket_e2e_test network -- --ignored --nocapture
```

### Тесты keep-alive
```bash
cargo test --test websocket_e2e_test keepalive -- --ignored --nocapture
```

### Стресс-тесты
```bash
cargo test --test websocket_e2e_test high_frequency -- --ignored --nocapture
```

## Требования

- Установите переменные окружения с API ключами:
  ```bash
  export DEEPGRAM_KEY="your-deepgram-api-key"
  export ASSEMBLY_AI_KEY="your-assemblyai-api-key"
  ```

- Или создайте файл `.env` в корне проекта:
  ```
  DEEPGRAM_KEY=your-deepgram-api-key
  ASSEMBLY_AI_KEY=your-assemblyai-api-key
  ```

## Покрытие тестов

### WebSocket Подключение
- ✅ Базовое подключение
- ✅ Переподключение после остановки
- ✅ Множественные подключения (проверка утечек)
- ✅ Abort во время активного соединения
- ✅ Обработка ошибок аутентификации

### Обработка Данных
- ✅ Отправка аудио чанков
- ✅ Получение partial транскрипций
- ✅ Получение final транскрипций
- ✅ Обработка пустых ответов
- ✅ Обработка Close сообщений

### Надежность
- ✅ Keep-Alive механизм
- ✅ Восстановление после разрыва
- ✅ Медленное соединение (300-500ms задержки)
- ✅ Быстрая отправка (10ms интервалы)
- ✅ Большие пачки данных (500ms чанки)
- ✅ Длинные сессии с паузами

### Производительность
- ✅ Стресс-тест высокочастотной отправки
- ✅ Сравнение Deepgram vs AssemblyAI
- ✅ Измерение времени отправки чанков
- ✅ Success rate при высокой нагрузке

## Особенности тестов

### Реалистичность
Тесты максимально приближены к реальному использованию:
- Имитация медленного интернета
- Паузы между записями (keep-alive)
- Длинные сессии с периодическими остановками
- Внезапные разрывы соединения

### Детальная диагностика
Каждый тест выводит подробную информацию:
- Время отправки чанков
- Количество полученных транскрипций
- Типы ошибок и их обработка
- Статистику производительности

### Безопасность
Все тесты с реальным API помечены `#[ignore]` и требуют явного запуска с флагом `--ignored`.

## Примеры вывода

### Успешный тест подключения
```
🔌 Устанавливаем соединение...
✅ WebSocket соединение установлено
📤 Отправляем данные...
✅ Соединение корректно закрыто
```

### Тест keep-alive
```
⏸️  Ставим на паузу (keep-alive)...
⏱️  Ждем 10 секунд (keep-alive работает в фоне)...
  1 сек...
  2 сек...
  ...
▶️  Возобновляем стрим...
✅ Keep-alive механизм работает корректно
```

### Стресс-тест
```
⚡ Стресс-тест: быстрая отправка данных (10ms интервалы)...
  Отправлено 20 чанков...
  Отправлено 40 чанков...
  ...
📊 Результаты:
  Успешно отправлено: 98
  Ошибок: 2
  Success rate: 98.0%
✅ Высокочастотная отправка работает стабильно
```

## Архитектура тестов

### Модульность
Каждый тест проверяет один конкретный аспект WebSocket networking.

### Независимость
Тесты не зависят друг от друга и могут запускаться в любом порядке.

### Воспроизводимость
Все тесты используют одинаковые тестовые данные и паттерны.

### Расширяемость
Легко добавить новые тесты используя существующие хелперы.

## Метрики качества

- **16** комплексных e2e тестов
- **4** категории покрытия
- **100%** покрытие основных WebSocket операций
- **2** провайдера (Deepgram, AssemblyAI)
- Тесты реальных сценариев использования

## Troubleshooting

### Тесты падают с ошибкой аутентификации
Проверьте что API ключи установлены правильно:
```bash
echo $DEEPGRAM_KEY
echo $ASSEMBLY_AI_KEY
```

### Тесты timeout
Увеличьте timeout в коде теста или проверьте интернет соединение.

### Assertion failures
Некоторые тесты могут быть чувствительны к качеству интернета. Запустите их повторно.

## Контрибьюция

При добавлении новых тестов:
1. Следуйте существующему стилю и структуре
2. Добавляйте подробные логи и диагностику
3. Маркируйте тесты с реальным API как `#[ignore]`
4. Обновляйте этот README

## CI/CD

В CI можно запускать только unit тесты (без API):
```bash
cargo test --test websocket_e2e_test test_e2e_connection_error
```

Для полного тестирования используйте secrets для API ключей:
```yaml
- name: Run WebSocket E2E tests
  env:
    DEEPGRAM_KEY: ${{ secrets.DEEPGRAM_KEY }}
    ASSEMBLY_AI_KEY: ${{ secrets.ASSEMBLY_AI_KEY }}
  run: cargo test --test websocket_e2e_test -- --ignored
```
